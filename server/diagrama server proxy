

Position  

Main{

  GameServer {
    -Se conecte un nuevo cliente.
    - Despachar una clase ClientProxy
    NPC npcs[];
    Jugadores jugadoresActivos[];
    ClasesClientProxy[];
    ObjetosMapa[{
      x, y, h, w
    }, {

    }, {

    }]
    run() {
      despacha un nuevo ClientProxy(
        ->this
      );
      ClasesClientProxy newInsance();
      ClasesClientProxy newInsance();
      ClasesClientProxy newInsance();


    }

    addActivePlayer(&Player) {
      mutex;
      jugadoresActivos.add(&Player)
    }
  }
}

-> Clase Server Proxy.
  &GameServer game;
  - Jugador.
  
  autentificarJugador() => {
    validar que exista)(); X

    game.addActivePlayer() {

    }
    si existe lo agrega a la lista.
  };



  run() {
    Jugador = autentificarJugador();
    
    if(!AABB.collision(jugador)) {
      jugador.mover(); 
    }

  }
